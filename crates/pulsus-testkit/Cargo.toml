[package]
name = "pulsus-testkit"
version = "0.0.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

# Deliberately dependency-free: every crate in the workspace pulls this in
# as a dev-dependency, so anything added here is compiled before any test
# binary can link. The whole crate is test support — it is never a
# `[dependencies]` entry of a production crate.
[dependencies]
