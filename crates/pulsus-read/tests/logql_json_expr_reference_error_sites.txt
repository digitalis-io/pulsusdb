# Issue #388 Stage B -- the reference's error sites in the `| json`
# extraction sub-grammar. THE LITERAL OUTPUT of one command, pasted, not
# curated:
#
#   git -C loki grep -n -E 'errors\.New\(|fmt\.Errorf\(|sc\.err = ' \
#       v3.7.4 -- pkg/logql/log/jsonexpr/ ':!*_test.go'
#
# WHAT THIS FILE IS. A **bounded inventory**, not a proof of
# completeness. Its scope is exactly: lines in NON-TEST files under
# pkg/logql/log/jsonexpr/ at v3.7.4 that contain an error-creating call
# spelled `errors.New(` or `fmt.Errorf(`, or an assignment spelled
# `sc.err = `.
#
# WHAT IS OUTSIDE IT, AND THE INSTANCE THAT WAS FOUND THERE. An error
# created by any other spelling, or in another package. That residue is
# NOT empty here, and the instance is recorded as a rule row in
# logql_json_expr_sites.tsv with source=FoundByProbe:
#
#   scanInt ends `return strconv.Atoi(string(number))` (lexer.go:153).
#   On an out-of-range integer that returns strconv's ErrRange, which
#   lexer.go:56 relays into sc.err and which SURVIVES whenever the parser
#   can accept the prefix it already shifted. Measured against the pinned
#   digest: `| json v="b 9223372036854775808]"` is 400 with
#   `strconv.Atoi: parsing "9223372036854775808": value out of range`,
#   `| json v="b[9223372036854775808]"` is 400 with the syntax error that
#   overwrites it, and `| json v="b[9223372036854775807]"` is 200.
#
# So the index bound is Go's `int` (i64 on every platform Loki ships
# for), not an unbounded integer -- a `usize` parse would have accepted
# 2^63 and introduced a divergence this change exists to close. Nothing
# in this repository could have put that line in this file: the command's
# scope is these two packages and the error is created in strconv.
#
# The pattern half's equivalent bound DOES hold, because
# pkg/logql/log/pattern/ imports only fmt, sync, bytes, errors and
# unicode/utf8, none of which returns a domain error to it. The two
# halves' completeness arguments are therefore different, which is one
# of the reasons their datasets are separate files.
#
# WHY IT STOPS HERE. Closing the spelling gap in general needs a
# source-structural enumeration -- parsing Go and following error-typed
# values to their returns. Nothing in this repository parses Go and CI
# cannot.
#
# Regenerated by re-running the command above at the pinned tag.
v3.7.4:pkg/logql/log/jsonexpr/lexer.go:26:	sc.err = fmt.Errorf("%s", s)
v3.7.4:pkg/logql/log/jsonexpr/lexer.go:56:				sc.err = fmt.Errorf("%s", err.Error())
v3.7.4:pkg/logql/log/jsonexpr/lexer.go:80:			sc.err = fmt.Errorf("unexpected char %c", r)
v3.7.4:pkg/logql/log/jsonexpr/lexer.go:114:		sc.err = fmt.Errorf("unexpected char %c", r)
v3.7.4:pkg/logql/log/jsonexpr/lexer.go:138:			return 0, fmt.Errorf("cannot use float as array index")
v3.7.4:pkg/logql/log/jsonexpr/lexer.go:147:			return 0, fmt.Errorf("non-integer value: %c", r)
