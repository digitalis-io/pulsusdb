== http_requests_total{status="500"} ==
params start_ms=1782907200000 end_ms=1782928800000 step_ms=60000
selectors 1
-- selector[0] name=http_requests_total range_ms=- window=(1782906900000, 1782928800000]
-- statement[000] offset=00000251 len=000276
SELECT fingerprint, unix_milli, value
FROM metric_samples
PREWHERE metric_name = 'http_requests_total'
WHERE unix_milli > 1782906900000 AND unix_milli <= 1782928800000
  AND fingerprint IN (toUInt128('101'), toUInt128('205'), toUInt128('990'))
ORDER BY fingerprint, unix_milli
-- statement[001] offset=00000573 len=000467
SELECT fingerprint, unix_milli, schema, zero_threshold, zero_count, count, sum, pos_span_offsets, pos_span_lengths, pos_bucket_deltas, neg_span_offsets, neg_span_lengths, neg_bucket_deltas, custom_values, counter_reset_hint
FROM metric_hist_samples
PREWHERE metric_name = 'http_requests_total'
WHERE unix_milli > 1782906900000 AND unix_milli <= 1782928800000
  AND fingerprint IN (toUInt128('101'), toUInt128('205'), toUInt128('990'))
ORDER BY fingerprint, unix_milli

== abs(http_requests_total{status="500"}) ==
params start_ms=1782907200000 end_ms=1782928800000 step_ms=60000
selectors 1
-- selector[0] name=http_requests_total range_ms=- window=(1782906900000, 1782928800000]
-- statement[002] offset=00001298 len=000276
SELECT fingerprint, unix_milli, value
FROM metric_samples
PREWHERE metric_name = 'http_requests_total'
WHERE unix_milli > 1782906900000 AND unix_milli <= 1782928800000
  AND fingerprint IN (toUInt128('101'), toUInt128('205'), toUInt128('990'))
ORDER BY fingerprint, unix_milli
-- statement[003] offset=00001620 len=000467
SELECT fingerprint, unix_milli, schema, zero_threshold, zero_count, count, sum, pos_span_offsets, pos_span_lengths, pos_bucket_deltas, neg_span_offsets, neg_span_lengths, neg_bucket_deltas, custom_values, counter_reset_hint
FROM metric_hist_samples
PREWHERE metric_name = 'http_requests_total'
WHERE unix_milli > 1782906900000 AND unix_milli <= 1782928800000
  AND fingerprint IN (toUInt128('101'), toUInt128('205'), toUInt128('990'))
ORDER BY fingerprint, unix_milli

== max by (status) (http_requests_total{status="500"}) ==
params start_ms=1782907200000 end_ms=1782928800000 step_ms=60000
selectors 1
-- selector[0] name=http_requests_total range_ms=- window=(1782906900000, 1782928800000]
-- grouped op=Max gids=[0, 1, 0]
-- statement[004] offset=00002391 len=002746
WITH 1782907200000 AS grid_start, 60000 AS grid_step, 361 AS grid_n, 300000 AS lookback,
     [toUInt128('101'), toUInt128('205'), toUInt128('990')] AS fps,
     CAST([0, 1, 0], 'Array(UInt32)') AS gids
SELECT gid, min(gi) AS gi_start, max(gi) AS gi_end, any(agg) AS agg, any(flags) AS flags
FROM (
  SELECT gid, gi, agg, flags,
    sum(is_new) OVER (PARTITION BY gid ORDER BY gi ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS run
  FROM (
    SELECT gid, gi, agg, flags,
      toUInt8(gi != lagInFrame(gi) OVER w + 1
              OR reinterpretAsUInt64(agg) != reinterpretAsUInt64(lagInFrame(agg) OVER w)
              OR flags != lagInFrame(flags) OVER w) AS is_new
    FROM (
      SELECT gid, gi,
        if(countIf(NOT is_hist AND NOT isNaN(v)) = 0, argMaxIf(v, fingerprint, NOT is_hist),
           maxIf(v, NOT is_hist AND NOT isNaN(v))) AS agg,
        toUInt8(if(countIf(NOT is_hist) > 0, 1, 0) + if(countIf(is_hist) > 0, 2, 0)) AS flags
      FROM (
        SELECT gid, fingerprint, v, is_hist,
          arrayJoin(range(
            toUInt32(least(toInt64(grid_n),
              if(ts <= grid_start, 0, intDiv(ts - grid_start + grid_step - 1, grid_step)))),
            toUInt32(least(toInt64(grid_n),
              if(cover_end <= grid_start, 0, intDiv(cover_end - grid_start + grid_step - 1, grid_step))))
          )) AS gi
        FROM (
          SELECT transform(fingerprint, fps, gids, CAST(0, 'UInt32')) AS gid, fingerprint,
            ts, v, is_hist, stale,
            least(leadInFrame(ts, 1, toInt64(1782928800000) + lookback + 1) OVER (
                    PARTITION BY fingerprint ORDER BY ts, is_hist
                    ROWS BETWEEN CURRENT ROW AND UNBOUNDED FOLLOWING),
                  ts + lookback) AS cover_end
          FROM (
            SELECT fingerprint, unix_milli AS ts, value AS v, CAST(0, 'UInt8') AS is_hist,
                   reinterpretAsUInt64(value) = 9218868437227405314 AS stale
            FROM metric_samples
            PREWHERE metric_name = 'http_requests_total'
            WHERE unix_milli > 1782906900000 AND unix_milli <= 1782928800000 AND fingerprint IN fps
            UNION ALL
            SELECT fingerprint, unix_milli AS ts, CAST(0, 'Float64') AS v, CAST(1, 'UInt8') AS is_hist,
                   reinterpretAsUInt64(sum) = 9218868437227405314 AS stale
            FROM metric_hist_samples
            PREWHERE metric_name = 'http_requests_total'
            WHERE unix_milli > 1782906900000 AND unix_milli <= 1782928800000 AND fingerprint IN fps
          )
        )
        WHERE NOT stale
      )
      GROUP BY gi, gid
    )
    WINDOW w AS (PARTITION BY gid ORDER BY gi ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW)
  )
)
GROUP BY gid, run
ORDER BY gid, gi_start

== min without (instance) (http_requests_total{status="500"}) ==
params start_ms=1782907200000 end_ms=1782928800000 step_ms=60000
selectors 1
-- selector[0] name=http_requests_total range_ms=- window=(1782906900000, 1782928800000]
-- grouped op=Min gids=[0, 1, 0]
-- statement[005] offset=00005448 len=002746
WITH 1782907200000 AS grid_start, 60000 AS grid_step, 361 AS grid_n, 300000 AS lookback,
     [toUInt128('101'), toUInt128('205'), toUInt128('990')] AS fps,
     CAST([0, 1, 0], 'Array(UInt32)') AS gids
SELECT gid, min(gi) AS gi_start, max(gi) AS gi_end, any(agg) AS agg, any(flags) AS flags
FROM (
  SELECT gid, gi, agg, flags,
    sum(is_new) OVER (PARTITION BY gid ORDER BY gi ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS run
  FROM (
    SELECT gid, gi, agg, flags,
      toUInt8(gi != lagInFrame(gi) OVER w + 1
              OR reinterpretAsUInt64(agg) != reinterpretAsUInt64(lagInFrame(agg) OVER w)
              OR flags != lagInFrame(flags) OVER w) AS is_new
    FROM (
      SELECT gid, gi,
        if(countIf(NOT is_hist AND NOT isNaN(v)) = 0, argMaxIf(v, fingerprint, NOT is_hist),
           minIf(v, NOT is_hist AND NOT isNaN(v))) AS agg,
        toUInt8(if(countIf(NOT is_hist) > 0, 1, 0) + if(countIf(is_hist) > 0, 2, 0)) AS flags
      FROM (
        SELECT gid, fingerprint, v, is_hist,
          arrayJoin(range(
            toUInt32(least(toInt64(grid_n),
              if(ts <= grid_start, 0, intDiv(ts - grid_start + grid_step - 1, grid_step)))),
            toUInt32(least(toInt64(grid_n),
              if(cover_end <= grid_start, 0, intDiv(cover_end - grid_start + grid_step - 1, grid_step))))
          )) AS gi
        FROM (
          SELECT transform(fingerprint, fps, gids, CAST(0, 'UInt32')) AS gid, fingerprint,
            ts, v, is_hist, stale,
            least(leadInFrame(ts, 1, toInt64(1782928800000) + lookback + 1) OVER (
                    PARTITION BY fingerprint ORDER BY ts, is_hist
                    ROWS BETWEEN CURRENT ROW AND UNBOUNDED FOLLOWING),
                  ts + lookback) AS cover_end
          FROM (
            SELECT fingerprint, unix_milli AS ts, value AS v, CAST(0, 'UInt8') AS is_hist,
                   reinterpretAsUInt64(value) = 9218868437227405314 AS stale
            FROM metric_samples
            PREWHERE metric_name = 'http_requests_total'
            WHERE unix_milli > 1782906900000 AND unix_milli <= 1782928800000 AND fingerprint IN fps
            UNION ALL
            SELECT fingerprint, unix_milli AS ts, CAST(0, 'Float64') AS v, CAST(1, 'UInt8') AS is_hist,
                   reinterpretAsUInt64(sum) = 9218868437227405314 AS stale
            FROM metric_hist_samples
            PREWHERE metric_name = 'http_requests_total'
            WHERE unix_milli > 1782906900000 AND unix_milli <= 1782928800000 AND fingerprint IN fps
          )
        )
        WHERE NOT stale
      )
      GROUP BY gi, gid
    )
    WINDOW w AS (PARTITION BY gid ORDER BY gi ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW)
  )
)
GROUP BY gid, run
ORDER BY gid, gi_start

== count(http_requests_total{status="500"}) ==
params start_ms=1782907200000 end_ms=1782928800000 step_ms=60000
selectors 1
-- selector[0] name=http_requests_total range_ms=- window=(1782906900000, 1782928800000]
-- grouped op=Count gids=[0, 1, 0]
-- statement[006] offset=00008489 len=002405
WITH 1782907200000 AS grid_start, 60000 AS grid_step, 361 AS grid_n, 300000 AS lookback,
     [toUInt128('101'), toUInt128('205'), toUInt128('990')] AS fps,
     CAST([0, 1, 0], 'Array(UInt32)') AS gids
SELECT gid, min(gi) AS gi_start, max(gi) AS gi_end, any(agg) AS agg
FROM (
  SELECT gid, gi, agg,
    sum(is_new) OVER (PARTITION BY gid ORDER BY gi ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS run
  FROM (
    SELECT gid, gi, agg,
      toUInt8(gi != lagInFrame(gi) OVER w + 1
              OR agg != lagInFrame(agg) OVER w) AS is_new
    FROM (
      SELECT gid, gi,
        toUInt32(count()) AS agg
      FROM (
        SELECT gid, fingerprint, v, is_hist,
          arrayJoin(range(
            toUInt32(least(toInt64(grid_n),
              if(ts <= grid_start, 0, intDiv(ts - grid_start + grid_step - 1, grid_step)))),
            toUInt32(least(toInt64(grid_n),
              if(cover_end <= grid_start, 0, intDiv(cover_end - grid_start + grid_step - 1, grid_step))))
          )) AS gi
        FROM (
          SELECT transform(fingerprint, fps, gids, CAST(0, 'UInt32')) AS gid, fingerprint,
            ts, v, is_hist, stale,
            least(leadInFrame(ts, 1, toInt64(1782928800000) + lookback + 1) OVER (
                    PARTITION BY fingerprint ORDER BY ts, is_hist
                    ROWS BETWEEN CURRENT ROW AND UNBOUNDED FOLLOWING),
                  ts + lookback) AS cover_end
          FROM (
            SELECT fingerprint, unix_milli AS ts, value AS v, CAST(0, 'UInt8') AS is_hist,
                   reinterpretAsUInt64(value) = 9218868437227405314 AS stale
            FROM metric_samples
            PREWHERE metric_name = 'http_requests_total'
            WHERE unix_milli > 1782906900000 AND unix_milli <= 1782928800000 AND fingerprint IN fps
            UNION ALL
            SELECT fingerprint, unix_milli AS ts, CAST(0, 'Float64') AS v, CAST(1, 'UInt8') AS is_hist,
                   reinterpretAsUInt64(sum) = 9218868437227405314 AS stale
            FROM metric_hist_samples
            PREWHERE metric_name = 'http_requests_total'
            WHERE unix_milli > 1782906900000 AND unix_milli <= 1782928800000 AND fingerprint IN fps
          )
        )
        WHERE NOT stale
      )
      GROUP BY gi, gid
    )
    WINDOW w AS (PARTITION BY gid ORDER BY gi ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW)
  )
)
GROUP BY gid, run
ORDER BY gid, gi_start

== group(http_requests_total{status="500"}) ==
params start_ms=1782907200000 end_ms=1782928800000 step_ms=60000
selectors 1
-- selector[0] name=http_requests_total range_ms=- window=(1782906900000, 1782928800000]
-- grouped op=Group gids=[0, 1, 0]
-- statement[007] offset=00011189 len=002399
WITH 1782907200000 AS grid_start, 60000 AS grid_step, 361 AS grid_n, 300000 AS lookback,
     [toUInt128('101'), toUInt128('205'), toUInt128('990')] AS fps,
     CAST([0, 1, 0], 'Array(UInt32)') AS gids
SELECT gid, min(gi) AS gi_start, max(gi) AS gi_end, any(agg) AS agg
FROM (
  SELECT gid, gi, agg,
    sum(is_new) OVER (PARTITION BY gid ORDER BY gi ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS run
  FROM (
    SELECT gid, gi, agg,
      toUInt8(gi != lagInFrame(gi) OVER w + 1
              OR agg != lagInFrame(agg) OVER w) AS is_new
    FROM (
      SELECT gid, gi,
        toUInt32(1) AS agg
      FROM (
        SELECT gid, fingerprint, v, is_hist,
          arrayJoin(range(
            toUInt32(least(toInt64(grid_n),
              if(ts <= grid_start, 0, intDiv(ts - grid_start + grid_step - 1, grid_step)))),
            toUInt32(least(toInt64(grid_n),
              if(cover_end <= grid_start, 0, intDiv(cover_end - grid_start + grid_step - 1, grid_step))))
          )) AS gi
        FROM (
          SELECT transform(fingerprint, fps, gids, CAST(0, 'UInt32')) AS gid, fingerprint,
            ts, v, is_hist, stale,
            least(leadInFrame(ts, 1, toInt64(1782928800000) + lookback + 1) OVER (
                    PARTITION BY fingerprint ORDER BY ts, is_hist
                    ROWS BETWEEN CURRENT ROW AND UNBOUNDED FOLLOWING),
                  ts + lookback) AS cover_end
          FROM (
            SELECT fingerprint, unix_milli AS ts, value AS v, CAST(0, 'UInt8') AS is_hist,
                   reinterpretAsUInt64(value) = 9218868437227405314 AS stale
            FROM metric_samples
            PREWHERE metric_name = 'http_requests_total'
            WHERE unix_milli > 1782906900000 AND unix_milli <= 1782928800000 AND fingerprint IN fps
            UNION ALL
            SELECT fingerprint, unix_milli AS ts, CAST(0, 'Float64') AS v, CAST(1, 'UInt8') AS is_hist,
                   reinterpretAsUInt64(sum) = 9218868437227405314 AS stale
            FROM metric_hist_samples
            PREWHERE metric_name = 'http_requests_total'
            WHERE unix_milli > 1782906900000 AND unix_milli <= 1782928800000 AND fingerprint IN fps
          )
        )
        WHERE NOT stale
      )
      GROUP BY gi, gid
    )
    WINDOW w AS (PARTITION BY gid ORDER BY gi ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW)
  )
)
GROUP BY gid, run
ORDER BY gid, gi_start

== topk(3, http_requests_total{status="500"}) ==
params start_ms=1782907200000 end_ms=1782928800000 step_ms=60000
selectors 1
-- selector[0] name=http_requests_total range_ms=- window=(1782906900000, 1782928800000]
-- statement[008] offset=00013850 len=000276
SELECT fingerprint, unix_milli, value
FROM metric_samples
PREWHERE metric_name = 'http_requests_total'
WHERE unix_milli > 1782906900000 AND unix_milli <= 1782928800000
  AND fingerprint IN (toUInt128('101'), toUInt128('205'), toUInt128('990'))
ORDER BY fingerprint, unix_milli
-- statement[009] offset=00014172 len=000467
SELECT fingerprint, unix_milli, schema, zero_threshold, zero_count, count, sum, pos_span_offsets, pos_span_lengths, pos_bucket_deltas, neg_span_offsets, neg_span_lengths, neg_bucket_deltas, custom_values, counter_reset_hint
FROM metric_hist_samples
PREWHERE metric_name = 'http_requests_total'
WHERE unix_milli > 1782906900000 AND unix_milli <= 1782928800000
  AND fingerprint IN (toUInt128('101'), toUInt128('205'), toUInt128('990'))
ORDER BY fingerprint, unix_milli

== count_values("v", http_requests_total{status="500"}) ==
params start_ms=1782907200000 end_ms=1782928800000 step_ms=60000
selectors 1
-- selector[0] name=http_requests_total range_ms=- window=(1782906900000, 1782928800000]
-- statement[010] offset=00014911 len=000276
SELECT fingerprint, unix_milli, value
FROM metric_samples
PREWHERE metric_name = 'http_requests_total'
WHERE unix_milli > 1782906900000 AND unix_milli <= 1782928800000
  AND fingerprint IN (toUInt128('101'), toUInt128('205'), toUInt128('990'))
ORDER BY fingerprint, unix_milli
-- statement[011] offset=00015233 len=000467
SELECT fingerprint, unix_milli, schema, zero_threshold, zero_count, count, sum, pos_span_offsets, pos_span_lengths, pos_bucket_deltas, neg_span_offsets, neg_span_lengths, neg_bucket_deltas, custom_values, counter_reset_hint
FROM metric_hist_samples
PREWHERE metric_name = 'http_requests_total'
WHERE unix_milli > 1782906900000 AND unix_milli <= 1782928800000
  AND fingerprint IN (toUInt128('101'), toUInt128('205'), toUInt128('990'))
ORDER BY fingerprint, unix_milli

== stddev by (status) (http_requests_total{status="500"}) ==
params start_ms=1782907200000 end_ms=1782928800000 step_ms=60000
selectors 1
-- selector[0] name=http_requests_total range_ms=- window=(1782906900000, 1782928800000]
-- statement[012] offset=00015974 len=000276
SELECT fingerprint, unix_milli, value
FROM metric_samples
PREWHERE metric_name = 'http_requests_total'
WHERE unix_milli > 1782906900000 AND unix_milli <= 1782928800000
  AND fingerprint IN (toUInt128('101'), toUInt128('205'), toUInt128('990'))
ORDER BY fingerprint, unix_milli
-- statement[013] offset=00016296 len=000467
SELECT fingerprint, unix_milli, schema, zero_threshold, zero_count, count, sum, pos_span_offsets, pos_span_lengths, pos_bucket_deltas, neg_span_offsets, neg_span_lengths, neg_bucket_deltas, custom_values, counter_reset_hint
FROM metric_hist_samples
PREWHERE metric_name = 'http_requests_total'
WHERE unix_milli > 1782906900000 AND unix_milli <= 1782928800000
  AND fingerprint IN (toUInt128('101'), toUInt128('205'), toUInt128('990'))
ORDER BY fingerprint, unix_milli

== histogram_quantile(0.5, http_requests_total{status="500"}) ==
params start_ms=1782907200000 end_ms=1782928800000 step_ms=60000
selectors 1
-- selector[0] name=http_requests_total range_ms=- window=(1782906900000, 1782928800000]
-- statement[014] offset=00017041 len=000276
SELECT fingerprint, unix_milli, value
FROM metric_samples
PREWHERE metric_name = 'http_requests_total'
WHERE unix_milli > 1782906900000 AND unix_milli <= 1782928800000
  AND fingerprint IN (toUInt128('101'), toUInt128('205'), toUInt128('990'))
ORDER BY fingerprint, unix_milli
-- statement[015] offset=00017363 len=000467
SELECT fingerprint, unix_milli, schema, zero_threshold, zero_count, count, sum, pos_span_offsets, pos_span_lengths, pos_bucket_deltas, neg_span_offsets, neg_span_lengths, neg_bucket_deltas, custom_values, counter_reset_hint
FROM metric_hist_samples
PREWHERE metric_name = 'http_requests_total'
WHERE unix_milli > 1782906900000 AND unix_milli <= 1782928800000
  AND fingerprint IN (toUInt128('101'), toUInt128('205'), toUInt128('990'))
ORDER BY fingerprint, unix_milli

== label_replace(http_requests_total{status="500"}, "l", "$1", "status", "(.*)") ==
params start_ms=1782907200000 end_ms=1782928800000 step_ms=60000
selectors 1
-- selector[0] name=http_requests_total range_ms=- window=(1782906900000, 1782928800000]
-- statement[016] offset=00018127 len=000276
SELECT fingerprint, unix_milli, value
FROM metric_samples
PREWHERE metric_name = 'http_requests_total'
WHERE unix_milli > 1782906900000 AND unix_milli <= 1782928800000
  AND fingerprint IN (toUInt128('101'), toUInt128('205'), toUInt128('990'))
ORDER BY fingerprint, unix_milli
-- statement[017] offset=00018449 len=000467
SELECT fingerprint, unix_milli, schema, zero_threshold, zero_count, count, sum, pos_span_offsets, pos_span_lengths, pos_bucket_deltas, neg_span_offsets, neg_span_lengths, neg_bucket_deltas, custom_values, counter_reset_hint
FROM metric_hist_samples
PREWHERE metric_name = 'http_requests_total'
WHERE unix_milli > 1782906900000 AND unix_milli <= 1782928800000
  AND fingerprint IN (toUInt128('101'), toUInt128('205'), toUInt128('990'))
ORDER BY fingerprint, unix_milli

== timestamp(http_requests_total{status="500"}) ==
params start_ms=1782907200000 end_ms=1782928800000 step_ms=60000
selectors 1
-- selector[0] name=http_requests_total range_ms=- window=(1782906900000, 1782928800000]
-- statement[018] offset=00019180 len=000276
SELECT fingerprint, unix_milli, value
FROM metric_samples
PREWHERE metric_name = 'http_requests_total'
WHERE unix_milli > 1782906900000 AND unix_milli <= 1782928800000
  AND fingerprint IN (toUInt128('101'), toUInt128('205'), toUInt128('990'))
ORDER BY fingerprint, unix_milli
-- statement[019] offset=00019502 len=000467
SELECT fingerprint, unix_milli, schema, zero_threshold, zero_count, count, sum, pos_span_offsets, pos_span_lengths, pos_bucket_deltas, neg_span_offsets, neg_span_lengths, neg_bucket_deltas, custom_values, counter_reset_hint
FROM metric_hist_samples
PREWHERE metric_name = 'http_requests_total'
WHERE unix_milli > 1782906900000 AND unix_milli <= 1782928800000
  AND fingerprint IN (toUInt128('101'), toUInt128('205'), toUInt128('990'))
ORDER BY fingerprint, unix_milli

== absent(http_requests_total{status="500"}) ==
params start_ms=1782907200000 end_ms=1782928800000 step_ms=60000
selectors 1
-- selector[0] name=http_requests_total range_ms=- window=(1782906900000, 1782928800000]
-- statement[020] offset=00020230 len=000276
SELECT fingerprint, unix_milli, value
FROM metric_samples
PREWHERE metric_name = 'http_requests_total'
WHERE unix_milli > 1782906900000 AND unix_milli <= 1782928800000
  AND fingerprint IN (toUInt128('101'), toUInt128('205'), toUInt128('990'))
ORDER BY fingerprint, unix_milli
-- statement[021] offset=00020552 len=000467
SELECT fingerprint, unix_milli, schema, zero_threshold, zero_count, count, sum, pos_span_offsets, pos_span_lengths, pos_bucket_deltas, neg_span_offsets, neg_span_lengths, neg_bucket_deltas, custom_values, counter_reset_hint
FROM metric_hist_samples
PREWHERE metric_name = 'http_requests_total'
WHERE unix_milli > 1782906900000 AND unix_milli <= 1782928800000
  AND fingerprint IN (toUInt128('101'), toUInt128('205'), toUInt128('990'))
ORDER BY fingerprint, unix_milli

== sort_desc(http_requests_total{status="500"}) ==
params start_ms=1782907200000 end_ms=1782907200000 step_ms=0
selectors 1
-- selector[0] name=http_requests_total range_ms=- window=(1782906900000, 1782907200000]
-- statement[022] offset=00021279 len=000276
SELECT fingerprint, unix_milli, value
FROM metric_samples
PREWHERE metric_name = 'http_requests_total'
WHERE unix_milli > 1782906900000 AND unix_milli <= 1782907200000
  AND fingerprint IN (toUInt128('101'), toUInt128('205'), toUInt128('990'))
ORDER BY fingerprint, unix_milli
-- statement[023] offset=00021601 len=000467
SELECT fingerprint, unix_milli, schema, zero_threshold, zero_count, count, sum, pos_span_offsets, pos_span_lengths, pos_bucket_deltas, neg_span_offsets, neg_span_lengths, neg_bucket_deltas, custom_values, counter_reset_hint
FROM metric_hist_samples
PREWHERE metric_name = 'http_requests_total'
WHERE unix_milli > 1782906900000 AND unix_milli <= 1782907200000
  AND fingerprint IN (toUInt128('101'), toUInt128('205'), toUInt128('990'))
ORDER BY fingerprint, unix_milli

== rate(http_requests_total{status="500"}[5m]) ==
params start_ms=1782907200000 end_ms=1782928800000 step_ms=60000
selectors 1
-- selector[0] name=http_requests_total range_ms=300000 window=(1782906600000, 1782928800000]
-- statement[024] offset=00022336 len=000276
SELECT fingerprint, unix_milli, value
FROM metric_samples
PREWHERE metric_name = 'http_requests_total'
WHERE unix_milli > 1782906600000 AND unix_milli <= 1782928800000
  AND fingerprint IN (toUInt128('101'), toUInt128('205'), toUInt128('990'))
ORDER BY fingerprint, unix_milli
-- statement[025] offset=00022658 len=000467
SELECT fingerprint, unix_milli, schema, zero_threshold, zero_count, count, sum, pos_span_offsets, pos_span_lengths, pos_bucket_deltas, neg_span_offsets, neg_span_lengths, neg_bucket_deltas, custom_values, counter_reset_hint
FROM metric_hist_samples
PREWHERE metric_name = 'http_requests_total'
WHERE unix_milli > 1782906600000 AND unix_milli <= 1782928800000
  AND fingerprint IN (toUInt128('101'), toUInt128('205'), toUInt128('990'))
ORDER BY fingerprint, unix_milli

== sum by (status) (rate(http_requests_total{status="500"}[5m])) ==
params start_ms=1782907200000 end_ms=1782928800000 step_ms=60000
selectors 1
-- selector[0] name=http_requests_total range_ms=300000 window=(1782906600000, 1782928800000]
-- statement[026] offset=00023411 len=000276
SELECT fingerprint, unix_milli, value
FROM metric_samples
PREWHERE metric_name = 'http_requests_total'
WHERE unix_milli > 1782906600000 AND unix_milli <= 1782928800000
  AND fingerprint IN (toUInt128('101'), toUInt128('205'), toUInt128('990'))
ORDER BY fingerprint, unix_milli
-- statement[027] offset=00023733 len=000467
SELECT fingerprint, unix_milli, schema, zero_threshold, zero_count, count, sum, pos_span_offsets, pos_span_lengths, pos_bucket_deltas, neg_span_offsets, neg_span_lengths, neg_bucket_deltas, custom_values, counter_reset_hint
FROM metric_hist_samples
PREWHERE metric_name = 'http_requests_total'
WHERE unix_milli > 1782906600000 AND unix_milli <= 1782928800000
  AND fingerprint IN (toUInt128('101'), toUInt128('205'), toUInt128('990'))
ORDER BY fingerprint, unix_milli

== max_over_time(http_requests_total{status="500"}[1m]) ==
params start_ms=1782907200000 end_ms=1782928800000 step_ms=60000
selectors 1
-- selector[0] name=http_requests_total range_ms=60000 window=(1782906840000, 1782928800000]
-- statement[028] offset=00024476 len=000276
SELECT fingerprint, unix_milli, value
FROM metric_samples
PREWHERE metric_name = 'http_requests_total'
WHERE unix_milli > 1782906840000 AND unix_milli <= 1782928800000
  AND fingerprint IN (toUInt128('101'), toUInt128('205'), toUInt128('990'))
ORDER BY fingerprint, unix_milli
-- statement[029] offset=00024798 len=000467
SELECT fingerprint, unix_milli, schema, zero_threshold, zero_count, count, sum, pos_span_offsets, pos_span_lengths, pos_bucket_deltas, neg_span_offsets, neg_span_lengths, neg_bucket_deltas, custom_values, counter_reset_hint
FROM metric_hist_samples
PREWHERE metric_name = 'http_requests_total'
WHERE unix_milli > 1782906840000 AND unix_milli <= 1782928800000
  AND fingerprint IN (toUInt128('101'), toUInt128('205'), toUInt128('990'))
ORDER BY fingerprint, unix_milli

== quantile_over_time(0.5, http_requests_total{status="500"}[10m]) ==
params start_ms=1782907200000 end_ms=1782928800000 step_ms=60000
selectors 1
-- selector[0] name=http_requests_total range_ms=600000 window=(1782906300000, 1782928800000]
-- statement[030] offset=00025553 len=000276
SELECT fingerprint, unix_milli, value
FROM metric_samples
PREWHERE metric_name = 'http_requests_total'
WHERE unix_milli > 1782906300000 AND unix_milli <= 1782928800000
  AND fingerprint IN (toUInt128('101'), toUInt128('205'), toUInt128('990'))
ORDER BY fingerprint, unix_milli
-- statement[031] offset=00025875 len=000467
SELECT fingerprint, unix_milli, schema, zero_threshold, zero_count, count, sum, pos_span_offsets, pos_span_lengths, pos_bucket_deltas, neg_span_offsets, neg_span_lengths, neg_bucket_deltas, custom_values, counter_reset_hint
FROM metric_hist_samples
PREWHERE metric_name = 'http_requests_total'
WHERE unix_milli > 1782906300000 AND unix_milli <= 1782928800000
  AND fingerprint IN (toUInt128('101'), toUInt128('205'), toUInt128('990'))
ORDER BY fingerprint, unix_milli

== avg_over_time(http_requests_total{status="500"}[5m] offset 1h) ==
params start_ms=1782907200000 end_ms=1782928800000 step_ms=60000
selectors 1
-- selector[0] name=http_requests_total range_ms=300000 window=(1782903000000, 1782925200000]
-- statement[032] offset=00026629 len=000276
SELECT fingerprint, unix_milli, value
FROM metric_samples
PREWHERE metric_name = 'http_requests_total'
WHERE unix_milli > 1782903000000 AND unix_milli <= 1782925200000
  AND fingerprint IN (toUInt128('101'), toUInt128('205'), toUInt128('990'))
ORDER BY fingerprint, unix_milli
-- statement[033] offset=00026951 len=000467
SELECT fingerprint, unix_milli, schema, zero_threshold, zero_count, count, sum, pos_span_offsets, pos_span_lengths, pos_bucket_deltas, neg_span_offsets, neg_span_lengths, neg_bucket_deltas, custom_values, counter_reset_hint
FROM metric_hist_samples
PREWHERE metric_name = 'http_requests_total'
WHERE unix_milli > 1782903000000 AND unix_milli <= 1782925200000
  AND fingerprint IN (toUInt128('101'), toUInt128('205'), toUInt128('990'))
ORDER BY fingerprint, unix_milli

== sum_over_time(http_requests_total{status="500"}[5m] @ 1782900000) ==
params start_ms=1782907200000 end_ms=1782928800000 step_ms=60000
selectors 1
-- selector[0] name=http_requests_total range_ms=300000 window=(1782899400000, 1782900000000]
-- statement[034] offset=00027708 len=000276
SELECT fingerprint, unix_milli, value
FROM metric_samples
PREWHERE metric_name = 'http_requests_total'
WHERE unix_milli > 1782899400000 AND unix_milli <= 1782900000000
  AND fingerprint IN (toUInt128('101'), toUInt128('205'), toUInt128('990'))
ORDER BY fingerprint, unix_milli
-- statement[035] offset=00028030 len=000467
SELECT fingerprint, unix_milli, schema, zero_threshold, zero_count, count, sum, pos_span_offsets, pos_span_lengths, pos_bucket_deltas, neg_span_offsets, neg_span_lengths, neg_bucket_deltas, custom_values, counter_reset_hint
FROM metric_hist_samples
PREWHERE metric_name = 'http_requests_total'
WHERE unix_milli > 1782899400000 AND unix_milli <= 1782900000000
  AND fingerprint IN (toUInt128('101'), toUInt128('205'), toUInt128('990'))
ORDER BY fingerprint, unix_milli

== rate(http_requests_total{status="500"}[5m:1m]) ==
params start_ms=1782907200000 end_ms=1782928800000 step_ms=60000
selectors 1
-- selector[0] name=http_requests_total range_ms=- window=(1782906600000, 1782928800000]
-- statement[036] offset=00028763 len=000276
SELECT fingerprint, unix_milli, value
FROM metric_samples
PREWHERE metric_name = 'http_requests_total'
WHERE unix_milli > 1782906600000 AND unix_milli <= 1782928800000
  AND fingerprint IN (toUInt128('101'), toUInt128('205'), toUInt128('990'))
ORDER BY fingerprint, unix_milli
-- statement[037] offset=00029085 len=000467
SELECT fingerprint, unix_milli, schema, zero_threshold, zero_count, count, sum, pos_span_offsets, pos_span_lengths, pos_bucket_deltas, neg_span_offsets, neg_span_lengths, neg_bucket_deltas, custom_values, counter_reset_hint
FROM metric_hist_samples
PREWHERE metric_name = 'http_requests_total'
WHERE unix_milli > 1782906600000 AND unix_milli <= 1782928800000
  AND fingerprint IN (toUInt128('101'), toUInt128('205'), toUInt128('990'))
ORDER BY fingerprint, unix_milli

== http_requests_total{status="500"}[5m] ==
params start_ms=1782907200000 end_ms=1782907200000 step_ms=0
selectors 1
-- selector[0] name=http_requests_total range_ms=300000 window=(1782906600000, 1782907200000]
-- statement[038] offset=00029810 len=000276
SELECT fingerprint, unix_milli, value
FROM metric_samples
PREWHERE metric_name = 'http_requests_total'
WHERE unix_milli > 1782906600000 AND unix_milli <= 1782907200000
  AND fingerprint IN (toUInt128('101'), toUInt128('205'), toUInt128('990'))
ORDER BY fingerprint, unix_milli
-- statement[039] offset=00030132 len=000467
SELECT fingerprint, unix_milli, schema, zero_threshold, zero_count, count, sum, pos_span_offsets, pos_span_lengths, pos_bucket_deltas, neg_span_offsets, neg_span_lengths, neg_bucket_deltas, custom_values, counter_reset_hint
FROM metric_hist_samples
PREWHERE metric_name = 'http_requests_total'
WHERE unix_milli > 1782906600000 AND unix_milli <= 1782907200000
  AND fingerprint IN (toUInt128('101'), toUInt128('205'), toUInt128('990'))
ORDER BY fingerprint, unix_milli

== time() ==
params start_ms=1782907200000 end_ms=1782928800000 step_ms=60000
selectors 0

== vector(1) ==
params start_ms=1782907200000 end_ms=1782928800000 step_ms=60000
selectors 0

== pi() ==
params start_ms=1782907200000 end_ms=1782928800000 step_ms=60000
selectors 0

== info(http_requests_total{status="500"}) ==
params start_ms=1782907200000 end_ms=1782928800000 step_ms=60000
selectors 2
-- selector[0] name=http_requests_total range_ms=- window=(1782906900000, 1782928800000]
-- statement[040] offset=00031132 len=000276
SELECT fingerprint, unix_milli, value
FROM metric_samples
PREWHERE metric_name = 'http_requests_total'
WHERE unix_milli > 1782906900000 AND unix_milli <= 1782928800000
  AND fingerprint IN (toUInt128('101'), toUInt128('205'), toUInt128('990'))
ORDER BY fingerprint, unix_milli
-- statement[041] offset=00031454 len=000467
SELECT fingerprint, unix_milli, schema, zero_threshold, zero_count, count, sum, pos_span_offsets, pos_span_lengths, pos_bucket_deltas, neg_span_offsets, neg_span_lengths, neg_bucket_deltas, custom_values, counter_reset_hint
FROM metric_hist_samples
PREWHERE metric_name = 'http_requests_total'
WHERE unix_milli > 1782906900000 AND unix_milli <= 1782928800000
  AND fingerprint IN (toUInt128('101'), toUInt128('205'), toUInt128('990'))
ORDER BY fingerprint, unix_milli
-- selector[1] name=target_info range_ms=- window=(1782906900000, 1782928800000]
-- statement[042] offset=00032048 len=000268
SELECT fingerprint, unix_milli, value
FROM metric_samples
PREWHERE metric_name = 'target_info'
WHERE unix_milli > 1782906900000 AND unix_milli <= 1782928800000
  AND fingerprint IN (toUInt128('101'), toUInt128('205'), toUInt128('990'))
ORDER BY fingerprint, unix_milli
-- statement[043] offset=00032362 len=000459
SELECT fingerprint, unix_milli, schema, zero_threshold, zero_count, count, sum, pos_span_offsets, pos_span_lengths, pos_bucket_deltas, neg_span_offsets, neg_span_lengths, neg_bucket_deltas, custom_values, counter_reset_hint
FROM metric_hist_samples
PREWHERE metric_name = 'target_info'
WHERE unix_milli > 1782906900000 AND unix_milli <= 1782928800000
  AND fingerprint IN (toUInt128('101'), toUInt128('205'), toUInt128('990'))
ORDER BY fingerprint, unix_milli

== rate(http_requests_total{status="500"}[5m]) / on (instance) rate(http_errors_total{status="500"}[5m]) ==
params start_ms=1782907200000 end_ms=1782928800000 step_ms=60000
selectors 2
-- selector[0] name=http_requests_total range_ms=300000 window=(1782906600000, 1782928800000]
-- statement[044] offset=00033147 len=000276
SELECT fingerprint, unix_milli, value
FROM metric_samples
PREWHERE metric_name = 'http_requests_total'
WHERE unix_milli > 1782906600000 AND unix_milli <= 1782928800000
  AND fingerprint IN (toUInt128('101'), toUInt128('205'), toUInt128('990'))
ORDER BY fingerprint, unix_milli
-- statement[045] offset=00033469 len=000467
SELECT fingerprint, unix_milli, schema, zero_threshold, zero_count, count, sum, pos_span_offsets, pos_span_lengths, pos_bucket_deltas, neg_span_offsets, neg_span_lengths, neg_bucket_deltas, custom_values, counter_reset_hint
FROM metric_hist_samples
PREWHERE metric_name = 'http_requests_total'
WHERE unix_milli > 1782906600000 AND unix_milli <= 1782928800000
  AND fingerprint IN (toUInt128('101'), toUInt128('205'), toUInt128('990'))
ORDER BY fingerprint, unix_milli
-- selector[1] name=http_errors_total range_ms=300000 window=(1782906600000, 1782928800000]
-- statement[046] offset=00034074 len=000274
SELECT fingerprint, unix_milli, value
FROM metric_samples
PREWHERE metric_name = 'http_errors_total'
WHERE unix_milli > 1782906600000 AND unix_milli <= 1782928800000
  AND fingerprint IN (toUInt128('101'), toUInt128('205'), toUInt128('990'))
ORDER BY fingerprint, unix_milli
-- statement[047] offset=00034394 len=000465
SELECT fingerprint, unix_milli, schema, zero_threshold, zero_count, count, sum, pos_span_offsets, pos_span_lengths, pos_bucket_deltas, neg_span_offsets, neg_span_lengths, neg_bucket_deltas, custom_values, counter_reset_hint
FROM metric_hist_samples
PREWHERE metric_name = 'http_errors_total'
WHERE unix_milli > 1782906600000 AND unix_milli <= 1782928800000
  AND fingerprint IN (toUInt128('101'), toUInt128('205'), toUInt128('990'))
ORDER BY fingerprint, unix_milli

== http_requests_total{status="500"} and http_requests_total{status="200"} ==
params start_ms=1782907200000 end_ms=1782928800000 step_ms=60000
selectors 2
-- selector[0] name=http_requests_total range_ms=- window=(1782906900000, 1782928800000]
-- statement[048] offset=00035150 len=000276
SELECT fingerprint, unix_milli, value
FROM metric_samples
PREWHERE metric_name = 'http_requests_total'
WHERE unix_milli > 1782906900000 AND unix_milli <= 1782928800000
  AND fingerprint IN (toUInt128('101'), toUInt128('205'), toUInt128('990'))
ORDER BY fingerprint, unix_milli
-- statement[049] offset=00035472 len=000467
SELECT fingerprint, unix_milli, schema, zero_threshold, zero_count, count, sum, pos_span_offsets, pos_span_lengths, pos_bucket_deltas, neg_span_offsets, neg_span_lengths, neg_bucket_deltas, custom_values, counter_reset_hint
FROM metric_hist_samples
PREWHERE metric_name = 'http_requests_total'
WHERE unix_milli > 1782906900000 AND unix_milli <= 1782928800000
  AND fingerprint IN (toUInt128('101'), toUInt128('205'), toUInt128('990'))
ORDER BY fingerprint, unix_milli
-- selector[1] name=http_requests_total range_ms=- window=(1782906900000, 1782928800000]
-- statement[050] offset=00036074 len=000276
SELECT fingerprint, unix_milli, value
FROM metric_samples
PREWHERE metric_name = 'http_requests_total'
WHERE unix_milli > 1782906900000 AND unix_milli <= 1782928800000
  AND fingerprint IN (toUInt128('101'), toUInt128('205'), toUInt128('990'))
ORDER BY fingerprint, unix_milli
-- statement[051] offset=00036396 len=000467
SELECT fingerprint, unix_milli, schema, zero_threshold, zero_count, count, sum, pos_span_offsets, pos_span_lengths, pos_bucket_deltas, neg_span_offsets, neg_span_lengths, neg_bucket_deltas, custom_values, counter_reset_hint
FROM metric_hist_samples
PREWHERE metric_name = 'http_requests_total'
WHERE unix_milli > 1782906900000 AND unix_milli <= 1782928800000
  AND fingerprint IN (toUInt128('101'), toUInt128('205'), toUInt128('990'))
ORDER BY fingerprint, unix_milli

== http_requests_total{status="500"} * 100 ==
params start_ms=1782907200000 end_ms=1782928800000 step_ms=60000
selectors 1
-- selector[0] name=http_requests_total range_ms=- window=(1782906900000, 1782928800000]
-- statement[052] offset=00037122 len=000276
SELECT fingerprint, unix_milli, value
FROM metric_samples
PREWHERE metric_name = 'http_requests_total'
WHERE unix_milli > 1782906900000 AND unix_milli <= 1782928800000
  AND fingerprint IN (toUInt128('101'), toUInt128('205'), toUInt128('990'))
ORDER BY fingerprint, unix_milli
-- statement[053] offset=00037444 len=000467
SELECT fingerprint, unix_milli, schema, zero_threshold, zero_count, count, sum, pos_span_offsets, pos_span_lengths, pos_bucket_deltas, neg_span_offsets, neg_span_lengths, neg_bucket_deltas, custom_values, counter_reset_hint
FROM metric_hist_samples
PREWHERE metric_name = 'http_requests_total'
WHERE unix_milli > 1782906900000 AND unix_milli <= 1782928800000
  AND fingerprint IN (toUInt128('101'), toUInt128('205'), toUInt128('990'))
ORDER BY fingerprint, unix_milli

== {__name__=~"http_.*",status="500"} ==
params start_ms=1782907200000 end_ms=1782928800000 step_ms=60000
selectors 1
-- selector[0] name=<none> range_ms=- window=(1782906900000, 1782928800000]
-- statement[054] offset=00038152 len=000326
SELECT metric_name, fingerprint, unix_milli, value
FROM metric_samples
PREWHERE metric_name IN ('http_requests_total', 'http_errors_total')
WHERE unix_milli > 1782906900000 AND unix_milli <= 1782928800000
  AND fingerprint IN (toUInt128('101'), toUInt128('205'), toUInt128('990'))
ORDER BY metric_name, fingerprint, unix_milli
-- statement[055] offset=00038524 len=000517
SELECT metric_name, fingerprint, unix_milli, schema, zero_threshold, zero_count, count, sum, pos_span_offsets, pos_span_lengths, pos_bucket_deltas, neg_span_offsets, neg_span_lengths, neg_bucket_deltas, custom_values, counter_reset_hint
FROM metric_hist_samples
PREWHERE metric_name IN ('http_requests_total', 'http_errors_total')
WHERE unix_milli > 1782906900000 AND unix_milli <= 1782928800000
  AND fingerprint IN (toUInt128('101'), toUInt128('205'), toUInt128('990'))
ORDER BY metric_name, fingerprint, unix_milli

