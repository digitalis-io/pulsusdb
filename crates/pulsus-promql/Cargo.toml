[package]
name = "pulsus-promql"
version = "0.0.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dev-dependencies]
# Issue #29 spike: validates `promql-parser` against the upstream Prometheus
# parser test corpus + an M2-subset AST golden matrix before #31 promotes it
# to a real `[dependencies]` entry. See docs/decisions/0002-promql-parser-
# selection.md.
promql-parser.workspace = true
serde.workspace = true
serde_json.workspace = true
sha2.workspace = true
